<br><br>
<footer>
  <div class="footer1">
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-2">
        </div>
        <div class="col-sm-4">
          <h1 class="title_footer">Deportes:</h1>
          <% if !@sports.nil? %>
            <div class="hidden-xs">
              <ul class="list-unstyled clear-margins">        
                <% @sports.each do |sport| %>
                  <li><%= link_to "#{sport.name}", sport_path(sport.id), :class=>"list-footer-item" %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </div>
      
        <div class="col-sm-4">
          <h1 class="title_footer">Ligas:</h1>
          <% if !@leagues.nil? %>          
            <div class="hidden-xs">
              <ul  class="list-unstyled clear-margins">
                <% @leagues.each do |league| %>
                  <li><%= link_to "#{league.name}", league_path(league.id), :class=>"list-footer-item" %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </div>

        <div class="col-sm-4">
          <h1 class="title_footer">Paises:</h1>
          <% if !@countries.nil? %>          
            <div class="hidden-xs">
              <ul  class="list-unstyled clear-margins">
                <% @countries.each do |country| %>
                  <li><%= link_to "#{country.name}", country_path(country.id), :class=>"list-footer-item" %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </div>

        <div class="col-sm-4">
          <div class="mm_logo_footer">
            <%= link_to(image_tag("logo.png", :alt => "Marcadores Mania", :height => "40px", :title => "Marcadores Mania"), "/") %> 
          </div>
        </div>

      </div>  
    </div>
  </div>

  <div class="footer2">
    <div class="container-fluid">
      <div class="row">

        <div class="col-sm-6">
          MARCADORES MANIA
        </div>
        <div class="col-sm-6">
          © Copyright 2017.
        </div>
        <div class="col-sm-6">
          <%= mail_to "info@marcadoresmania.com" ,nil,:class => "footer_mail" %>
        </div>

      </div>
    </div>
  </div>
</footer>